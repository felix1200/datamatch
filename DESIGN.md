# DESIGN.md

## Design Tokens

### 色彩
- 主色：Excel 绿 `#1D6F42`，象征电子表格与数据处理
- 辅助色：浅绿 `#E8F5E9`，用于高亮和选中状态
- 背景：纯白 `#FFFFFF` + 浅灰 `#F8FAFB`，营造干净的数据工具氛围
- 文字：深灰 `#1A1A2E` 主文字，`#6B7280` 次要文字
- 边框：`#E5E7EB`，保持视觉轻盈

### 字体
- 数据展示：等宽字体 `JetBrains Mono`（Google Fonts），确保数字对齐
- 界面文字：系统默认中文字体栈

### 圆角
- 卡片：`12px`
- 按钮/输入框：`8px`
- 表格单元格：`4px`

## 布局与响应式
- 最大宽度 `1200px` 居中
- 步骤式布局：上传 → 预览 → 配置 → 输出，纵向流程清晰
- 数据预览表格：横向滚动，固定首列

## 交互与状态
- 拖拽上传区域：虚线边框 + 悬停高亮
- 公式输出：代码块样式，一键复制带反馈动画
- 匹配结果：成功绿色高亮，未匹配红色标记

## 设计禁忌
- 不使用渐变或复杂阴影
- 不使用深色模式（数据工具以浅色为主）
- 避免过多动画干扰数据阅读
